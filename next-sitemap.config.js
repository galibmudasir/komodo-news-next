/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://komodo-news.vercel.app/", // Ganti dengan domain kamu
  generateRobotsTxt: true, // true = auto bikin robots.txt
  // Optional: exclude halaman tertentu
  // exclude: ['/admin', '/login'],
};
